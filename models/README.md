# Models

Files for models produced with **3D printers, laser cutters or CNC machines**
(e.g. `.stl`, `.step`, `.dxf`) used to build the vehicle's parts.

If there is nothing to add here, this folder may be removed.
